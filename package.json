{
  "name": "koars-config",
  "version": "3.0.0",
  "main": "index.js",
  "description": "Loading of config files",
  "repository": {
    "type": "git",
    "url": "https://github.com/koars/config.git"
  },
  "devDependencies": {
    "jshint": "^2.5.6",
    "jshint-stylish": "^1.0.0",
    "mocha": "^1.21.4",
    "mock-fs": "^2.3.1",
    "must": "^0.12.0"
  },
  "scripts": {
    "test": "mocha --require must && jshint . --exclude-path .gitignore --reporter node_modules/jshint-stylish/stylish.js"
  },
  "author": "Florian Albertz",
  "license": "BSD-3-Clause",
  "jshintConfig": {
    "node": true,
    "mocha": true
  },
  "dependencies": {
    "merge-defaults": "^0.2.1"
  }
}
